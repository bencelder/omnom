To run the web server, in the same folder as index.html type
python -m CGIHTTPServer 8000

Consider using a web framework, like Django (large, many features) or flask (smaller, lean).

For publishing, maybe heroku or aws or self-hosted
